#include <stdlib.h>
#include <stdio.h>
/**
 * mul - multiplication of two intigers
 * @a: for first int
 * @b: for second int
 * Return: multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}
